services:
  web:
    image: wycliffeassociates/linting-results:${IMAGE_TAG}
    environment:
      - ServiceBusConnectionString=${ServiceBusConnectionString}
      - ConnectionStrings__DefaultConnection="DataSource=/app/data/database.db;Cache=Shared"
    volumes:
      - data:/app/database.db

volumes:
  data: